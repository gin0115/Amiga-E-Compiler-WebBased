# --- third-party / copyrighted material we do not own: never push ---

# the harvested Aminet corpus, compiler distributions, extracted archives (486MB)
research/

# the browser IDE lives locally / in a separate package — this repo is the
# compiler only. (modules/ at the root IS tracked: the E v40 binary modules,
# included with Wouter van Oortmerssen's kind permission.)
web/

# --- local machine noise ---
.claude/
node_modules/
*.log
